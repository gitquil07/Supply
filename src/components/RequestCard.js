import styled from "styled-components";

export const RequestCard = ({id}) => {
    return (
        <RequestBlank>
        </RequestBlank>

    );
}

const RequestBlank = styled.div`
    padding:10px;
    border-radius:5px;
    border:1px solid rgba(0, 0, 0, 0.1);
    background-color:#F6F6FC;
`;